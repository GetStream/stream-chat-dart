name: stream_chat_example
description: example Flutter project using Stream Chat service

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^4.0.2
  rxdart: ^0.23.1
  flutter_widgets: ^0.1.10
  timeago: ^2.0.26
  date_format: ^1.0.8

  stream_chat:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.7.0
  build_web_compilers: ^1.7.0

flutter:
  uses-material-design: true
